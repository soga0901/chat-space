- @messages.each do |message|
  %li#js-message
    %h3.message__user__name
      = message.user.name
    %h4.message__date
      = message.created_at
    %p.message__text
      = message.body
    %p.message__image
      = image_tag message.image.to_s
