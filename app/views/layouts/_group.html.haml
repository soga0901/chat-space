- @groups.each do |group|
  = link_to group_messages_path(group), class: "link-btn" do
    .chat__list
      %h3.chat__room__name
        = group.name
      %p.chat__room__message
        = group.last_message
